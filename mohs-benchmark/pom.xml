<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.mohs</groupId>
        <artifactId>mohs-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>mohs-benchmark</artifactId>

    <name>Mohs :: Benchmark</name>
    <!--
      Bancada de performance (ARCHITECTURE_REDESIGN_PLAN.md, Phase 0): scripts de
      medição macro (write amplification, drains fim a fim) e, futuramente, os
      harnesses JMH/EXPLAIN hoje espalhados nos test sources de mohs-jdbc.
      Nunca publicado — mesmo estatuto do mohs-demo.
    -->
    <description>Load, JMH and EXPLAIN harnesses; never published</description>

    <properties>
        <maven.deploy.skip>true</maven.deploy.skip>
    </properties>

    <dependencies>
        <!-- Tudo escopo test: o módulo não tem código de produção — os harnesses moram
             em src/test e rodam só por -Dtest= explícito (padrão surefire não casa
             *Harness), como já era em mohs-jdbc. -->
        <dependency>
            <groupId>io.mohs</groupId>
            <artifactId>mohs-store-jdbc</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Os *TestSupport (containers por dialeto) de mohs-jdbc — type=test-jar não é
             coberto pelo dependencyManagement do jar, daí a versão explícita. -->
        <dependency>
            <groupId>io.mohs</groupId>
            <artifactId>mohs-store-jdbc</artifactId>
            <version>${project.version}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.mohs</groupId>
            <artifactId>mohs-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.microsoft.sqlserver</groupId>
            <artifactId>mssql-jdbc</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-mssqlserver</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-mysql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
