{
  "_comment": "Merge this 'hooks' block into .claude/settings.json. Replaces the bash version (require-pipeline.sh) with the cross-platform Node one.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"$CLAUDE_PROJECT_DIR/.claude/hooks/require-pipeline.mjs\""
          }
        ]
      }
    ]
  }
}
