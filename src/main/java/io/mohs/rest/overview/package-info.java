/**
 * Área de recurso "overview": {@link io.mohs.rest.overview.OverviewController}
 * ({@code GET /overview}, âncora de polling do dashboard) e seus DTOs —
 * {@link io.mohs.rest.overview.OverviewResponse},
 * {@link io.mohs.rest.overview.ThroughputView}. Depende de
 * {@code io.mohs.core.execution} (tipo reaproveitado direto:
 * {@code ExecutionState}).
 */
@NullMarked
package io.mohs.rest.overview;

import org.jspecify.annotations.NullMarked;
