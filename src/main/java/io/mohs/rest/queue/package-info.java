/**
 * Área de recurso "queues": {@link io.mohs.rest.queue.QueuesController}
 * ({@code GET}/{@code PATCH /queues/{name}}, cluster-wide) e seus DTOs —
 * {@link io.mohs.rest.queue.QueueResponse}, {@link io.mohs.rest.queue.QueuePatchRequest}.
 * Depende de {@code io.mohs.rest} ({@code RuntimePatchResponse}, envelope
 * compartilhado de todo endpoint de ajuste runtime).
 */
@NullMarked
package io.mohs.rest.queue;

import org.jspecify.annotations.NullMarked;
