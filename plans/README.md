# Implementation Plans

Generated by the `improve` skill on 2026-08-13. Execute in the order below
unless dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

Context for whoever picks these up: as of 2026-08-13 this repository has no
commits yet — it's a Spring Initializr skeleton (`MohsApplication.java` +
one trivial test) sitting alongside an extensively pre-designed spec in
`docs/` (`MOHS-DOCUMENTO-MESTRE.md`, `API-DESIGN.md`, `REST-API-DESIGN.md`)
that plans milestones M0-M4. These five plans close the gap between the
already-decided design and the actual bootstrap, before real M1 code makes
that gap more expensive to close. None of them add engine/domain logic —
that's M1+, out of scope here.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Rename Maven coordinates and root package to `io.mohs` | P1 | S | — | DONE (924334a) |
| 002  | M0 package skeleton + ArchUnit boundary tests | P1 | S–M | 001 | DONE (see merge commit) |
| 003  | Mark `spring-boot-starter-webmvc` optional | P2 | S | — | DONE (cf54696) |
| 004  | Disable `spring.jpa.open-in-view` | P2 | S | — | DONE (135f588) |
| 005  | Write `docs/adr/` files for the 12 decided ADRs | P3 | M | — | DONE (see merge commit) |

## Dependency notes

- 002 requires 001 because it creates `io.mohs.engine`, `io.mohs.jdbc`,
  `io.mohs.autoconfigure`, `io.mohs.rest`, `io.mohs.test` as siblings of the
  `io.mohs` root package — building the skeleton under the wrong groupId/
  package would mean redoing the move twice.
- 003, 004, and 005 are independent of each other and of 001/002. They're
  listed after 001/002 by priority (P1 before P2/P3), not because of a hard
  dependency — an executor could do them in any order, or in parallel across
  separate sessions, without conflict (each touches a disjoint set of
  files: 003 and 004 both touch different keys, in different files, of the
  same low-risk config surface; 005 touches only `docs/adr/`).

## Findings considered and rejected

None rejected — all five audited findings were selected for planning by the
user. See the audit conversation for two smaller findings that were
mentioned but not separately planned (folded into existing plans instead):
placeholder `pom.xml` metadata (`<url/>`, `<licenses>`, `<developers>`,
`<scm>`) was noted as low-priority cosmetic cleanup, not selected for its
own plan.
